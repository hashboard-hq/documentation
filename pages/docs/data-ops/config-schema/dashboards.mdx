---
description: Configuration file schema for Dashboards
---

import { Callout } from "nextra-theme-docs";
import EmbedDocsFromHashboardApp from "/components/EmbedDocsFromHashboardApp";

# Dashboards

<Callout type="warning">
  Hashboard previously used the term **saved view** to refer to what is now called a **saved exploration**. 
  At the moment our configuration files still reference **saved views**. 
  We plan to update the schema of these configuration files in the future.
</Callout>

<br/>

<EmbedDocsFromHashboardApp path="publicResourceSpecs/Dashboard" />
